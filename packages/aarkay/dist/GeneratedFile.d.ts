export default class GeneratedFile {
    modules: string[];
    destination: string;
    directory: string;
    name: string;
    template: string;
    context: object;
    constructor(modules: string[], destination: string, directory: string, name: string, template: string, context: object);
    render(): void;
}
